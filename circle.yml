machine:
  node:
    version: 4.2.2

dependencies:
  override:
    - npm install bower -g

test:
  override:
    - make
  post:
    - 'find build -type d -name .git -exec rm -rf {} +'
    - 'find build -type f -name .git\* -exec rm {} +'
    - tar cfjv $CIRCLE_ARTIFACTS/docs.tar.bz2 build/

